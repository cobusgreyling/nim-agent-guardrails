name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[nim,dev]"
          pip install pytest

      - name: Run tests
        run: |
          pytest tests/ -v --tb=short

      - name: Test import and version
        run: |
          python -c "from nim_guardrails import __version__; print('Version:', __version__); from nim_guardrails import GuardedAgent, GuardrailChain; print('Core imports OK')"
